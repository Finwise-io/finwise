# Finwise Bug Ledger

Single source of truth for bugs and design questions surfaced by the comprehensive-testing effort
(see `docs/finwise-launch-test-plan.md` for the umbrella plan). Conventions:

- Every newly **failing** test gets a row here BEFORE any fix lands.
- Questionable-but-current behavior gets a **documenting test** (passes today, pins behavior) and a `by-design?` status until a decision is made.
- A bug counts as "identified" once it has a row + a committed exposing/documenting test, even if the fix is deferred.

Statuses: `open` · `fixed` · `by-design?` · `by-design` · `deferred`

Baseline (2026-06-12, commit 4dbfe77): 264/264 tests green, `tsc --noEmit` clean.
Closeout (2026-06-12): comprehensive-testing pass complete — **494 tests** (264 → 494: invariants,
journeys, store seeding, services, shared utils, UI components, all-28-screen smoke + deep screens,
edge extremes), both jest projects + `tsc` green. Open rows below each name their exposing test.

**Summary (updated 2026-06-23, B-L review R-1): ALL 42 tracked rows are `fixed` — 0 open.** The four
once-open (B-24, B-18, B-19, B-25) were all fixed 2026-06-13; this header previously lagged that. No
open user-facing money error remains. (Earlier dispositions kept for history: 2 HIGH B-15/B-16 fixed;
B-17/B-20 were by-design; B-22 closed not-a-bug — all now reflected as fixed/closed in the table.)

> 2026-06-13: B-21 and B-23 resolved (see rows). B-21 (was by-design?) now seeds a $0 placeholder
> asset; B-23 (was deferred) closed by removing the currency picker and locking the app to USD until
> a per-country tax/retirement engine exists.
> 2026-06-13 (later): high-effort review-hardening pass on the B-21/B-23/B-49 diff fixed 6 findings —
> unified net-worth basis via `resolveNetWorthRows` (delete-everything now shows $0, not stale
> onboarding), `buildSnapshot` rows made required (+ `snapshotFromOnboarding`), TopBar off the
> retirement Monte-Carlo, `assetSheetReady` $0 guard, whitespace presence guard, earmark-basis test.
> See finwise-fix-list.md §24. **545 tests green, `tsc` clean.**

User-lens QA walk (2026-06-12, docs/finwise-userlens-qa-2026-06-12.md, 34 test cases: 21 pass /
9 fail / 4 pass-with-notes) added **B-28..B-36**: 2 HIGH (B-34 salary smoothing corrupts
Home/Budget/goal-capacity; B-31 cockpit ignores captured SS/pension) + 4 MED + 3 LOW. Suggested
fix order in the report.

Persona walk addendum (retiree / student / gig, 15 test cases: 10 pass / 5 fail) reproduced
B-28 on ALL personas (highest-frequency bug), reproduced B-34 on the gig worker (flat income chart
for swing pay), reproduced B-31 worse for an actually-retired user, **scoped B-33 to the
lump-monthlySpending-no-categories path** (didn't reproduce where categories exist), and added
**B-37** (retiree cockpit shows accumulation framing). Confirmed working: student tuition-crunch
planner + gig lean-month calendar are accurate.

**Prior-testing reconciliation (2026-06-12):** folded in findings from earlier QA docs so this
ledger covers prior testing too, not just this session. The onboarding **screen review**
(`finwise-onboarding-screen-review.md`, 6 P1/6 P2/8 P3) and **flow review**
(`finwise-onboarding-flow-review.md`) were **fixed** in commits d3268ce / f2ec7a5 / 88b5117
(decimal-pad inputs, dead `employerContribution` screen wired, `retLocation`→COL, recap screens,
household-first ordering, etc.) — verified, no open rows needed. The **data review**
(`finwise-data-review.md`) was mostly resolved (travelBudget/medicalBudget/spendingChangeLater now
feed `retirementSpendMonthly`; actual dividend/coupon income now in IncomeDetail; cashflow consumes
budget.spendByMonth), **but six findings remain open → added as B-38..B-43** (verified still open
against the code on 2026-06-12).

| ID | Severity | Area | Symptom | Root cause | Exposing test | Status | Notes |
|----|----------|------|---------|------------|---------------|--------|-------|
| B-15 | HIGH | Onboarding→Net Worth | Re-running onboarding never updates Net Worth accounts; stale balances drive retirement math | `seedNetWorth` one-time guard (`src/store/useStore.ts:536`): `s.nwSeeded ? {} : {...}` | `journeys.test.ts` "re-seeding with updated answers", `store_networth.test.ts` | **fixed** 2026-06-12 | = user-test feedback #15. Fixed via origin-tagged merge: seeded rows carry `origin: 'onboarding'`; re-seed replaces only those, manual accounts never touched, legacy untagged rows deduped by label+bucket |
| B-16 | HIGH | Onboarding restart | `restartOnboarding` wiped ALL asset accounts/liabilities incl. user-created ones, contradicting its own comment | Blanket wipe at `src/store/useStore.ts` restartOnboarding; no per-account origin flag existed | `journeys.test.ts` "manual account survives restart", `store_networth.test.ts` | **fixed** 2026-06-12 | Same origin-tag mechanism: restart clears only `origin: 'onboarding'` rows |
| B-17 | LOW | Income gate | Gate is on source selection, not amounts — but $0 amounts still yield $0 income, so selection alone can't fabricate income | `currentRetirementIncomeMonthly` (`src/domain/income/onboarding.ts:171-175`) | `invariants.test.ts` "selecting retirement_income with $0 amounts" | by-design | Verified harmless; gate correctly blocks the working-40yo future-SS leak both ways |
| B-18 | MED | Market data | Network failure, invalid ticker, and malformed response all silently return `null` → stale cached prices shown with no staleness indicator, no retry, no ticker validation | `src/services/marketData.ts` fetchSeries try/catch → null; store `refreshPrices` falls back to cache silently | `marketData.test.ts` priceFreshness + isPlausibleTicker | **fixed** 2026-06-13 | Performance screen shows price freshness ("updated Nh ago" / "⚠ may be out of date · tap Refresh" past ~4 days) + flags implausible tickers in the holding editor. (Offline-vs-invalid distinction is a deeper provider refactor, left noted.) |
| B-19 | MED | Net worth accuracy | Position with a ticker missing from the price cache contributes $0 to account balance → silent net-worth understatement | `recomputeBalances` (`src/store/useStore.ts:13-20`) treats missing price as 0 | `store_prices.test.ts` "missing price falls back to cost basis" | **fixed** 2026-06-13 | `recomputeBalances` falls back to `costBasis(p)` when the live price is absent, instead of $0 |
| B-20 | LOW | Goals capacity | Negative capacity (invest > surplus) — divide-by-negative risk in months-to-goal | `buildGoalsState` guards with `> 0` → `months_to_goal: null`; snapshot also clamps (`src/domain/snapshot.ts:60`) | `invariants.test.ts` negative-capacity probe | by-design | Verified guarded; negative capacity passes through for display but never divides |
| B-24 | MED | Budget/Goals consistency | User states total spend AND itemizes part of it → month grid honored the full total, but `projected_to_save` (→ goal capacity) only saw the categorized part; goal pool overstated free cash by the uncategorized remainder | `budgetFromOnboarding` took bucket total when > 0, ignoring the `monthlySpending` remainder that `spendByMonth` adds | `invariants.test.ts` "budget matches the month grid (B-24 fixed)" | **fixed** 2026-06-13 | `monthly_spending: max(bucket total, monthlySpending)` — budget surplus now equals the month-grid savings; goal capacity no longer overstated |
| B-21 | LOW | Onboarding seeding | Explicit $0 answers for retirement savings / holdings created no account at all (vs. a $0 account the user could edit later) | `assetsFromOnboarding` `bal > 0` filter (`src/domain/assets/index.ts`) | `journeys.test.ts` "explicit $0 asset answers seed $0 placeholder accounts" + `store_networth.test.ts` B-21 row | **fixed** 2026-06-13 | `add` now keys on field PRESENCE not value: an answered field (incl. explicit `'0'`) seeds a $0 placeholder; an absent field (e.g. `currentSavingsPortfolio` in the accumulation flow) is still skipped. `AssetSheet` save relaxed to `amt >= 0` so $0 accounts can be added/kept. Assets only — liabilities/goals/income still drop $0 (a $0 placeholder there is clutter) |
| B-22 | LOW | Retirement solver | Suspected non-monotonic `solveRetireAge` results from per-iteration rounding | `src/domain/retirement/index.ts` solver loop | `edge_extremes.test.ts` "solveRetireAge is monotonic in starting balance" | closed — not a bug | Probe ran across 6 balance levels (100k→3.2M): strictly monotonic; rounding happens only on outputs |
| B-25 | LOW | Economic data | Fallback inflation/treasury values (BLS/Treasury outage) carry no flag, so projections silently run on defaults and the UI can't label them as estimates | `fetchEconomicData` (`src/services/economicData.ts:28-39`) returns the same shape for live and fallback | `economicData.test.ts` "fallback values are flagged" + partial-outage | **fixed** 2026-06-13 | Added per-source `inflationIsFallback`/`treasuryIsFallback` flags + exported FALLBACK_* constants. (fetchEconomicData isn't wired into the UI yet, so no live surface needed) |
| B-34 | HIGH | Income accuracy | IncomeState salary = max(salaryByMonth)×12 — six $0 months ignored → Home take-home, IncomeDetail gross/net, Budget projected_to_save, and one-pool goal capacity all overstated (~$60k/yr gross for Jane); tax organizer & bill calendar use the true table → screens contradict | `incomeFromOnboarding` booked `grossSalaryMonthly` (max month) as flat MONTHLY | `income.test.ts` "B-34: gapped salary total equals the table sum" + tips + flat-salary guard | **fixed** 2026-06-12 | Booked `salaryAnnual(op)/12` (and `tipsAnnual(op)/12`) as MONTHLY so the total equals the real table sum and matches `totalGrossAnnual`; employer-match annualization preserved. Verified: Jane IncomeState 336,510 → 276,510 = totalGrossAnnual |
| B-31 | HIGH | Retirement handoff | Cockpit computes with $0 guaranteed income and asks "Are you eligible?" although onboarding captured ri_ss $2,000 + ri_pension $1,500; pension never consumed at all; snapshot surfaces DO count it → contradictory advice ("keep saving $3,775/mo") | `RetirementCockpit.tsx` `ssIncome = A.ssEligible ? … : 0`; `ssDefault` only read `ri_ss` | `screens_deep.test.tsx` "SS + pension from onboarding surface as guaranteed income" + no-income-prompt test | **fixed** 2026-06-13 | Guaranteed default now `retirementIncomeMonthly(op)` (ss+pension+annuities…); unset eligibility falls back to that figure (only explicit "Not eligible" zeroes it); row relabeled "Social Security & pension" and shows the amount "from your setup" |
| B-28 | MED | Goals/Plan tab | "Typical free cash to save $17,250/mo" = income BEFORE spending; true free cash $2,250/mo | `GoalsScreen` used `incomeMonthlyGrid(op,'available')` instead of `savingsByMonth(op)` | `screens_deep.test.tsx` "free-cash reflects savings not raw income" + deficit-hides-card | **fixed** 2026-06-13 | Switched to `savingsByMonth(op)` (income − spend). Sim now shows negative lean months (impossible with the income grid), confirming the fix |
| B-29 | MED | Goals handoff | Onboarding goals (2 set) never appear on Plan tab ("No goals yet"); Sharpen checklist nags "Add a goal" | `GoalsScreen` read `store.goals` only; `op.goals` unconsumed | `store_networth.test.ts` "seedGoals" suite (4 tests) + `screens_deep.test.tsx` "onboarding goals appear on Plan tab" | **fixed** 2026-06-13 | New `seedGoals(op)` store action (origin-tagged, seed-once via `goalsSeeded`, restart clears+re-seeds, manual goals preserved). Called at onboarding finish AND on Plan-tab mount. Verified live: Jane's Emergency fund $36k + House $45k now show |
| B-35 | MED | Tax organizer | 2026 taxable income included $42,000 SS/pension the (non-retired) user does not receive — the SS-leak gate was bypassed on this screen | `taxOrganizer` used un-gated `retirementIncomeMonthly` | `planning.test.ts` "future SS/pension NOT in working person's taxable income" + retiree-does-report | **fixed** 2026-06-13 | Switched to `currentRetirementIncomeMonthly` (same gate as the rest of the app); working person's taxable total unchanged by future SS, retiree still reports it |
| B-33 | MED | Stress test | Same screen said "$3,000 hit puts you in the red" AND "✓ you already have 3 months of essentials — strong cushion" | `monthlyEssentials()` returned $0 when spendCats empty (ignored monthlySpending) → recommendedFund $0 → gapToFund 0 → false cushion | `budget.test.ts` "lump monthlySpending with no categories falls back" | **fixed** 2026-06-13 | `monthlyEssentials` falls back to `max(bucket total, monthlySpending)` when uncategorized; gapToFund now positive so the screen shows the real shortfall, not "strong cushion" |
| B-30 | LOW | DTI guideline | Mortgage holder graded with renter guideline (20% → "High" at 33% instead of homeowner 36%) | `debtsFromOnboarding` hardcoded `debt_type:'OTHER'`; `GoalsScreen` homeowner check needs MORTGAGE type | `modules.test.ts` "infers debt type from the name" | **fixed** 2026-06-13 | New `inferDebtType(label)` maps Mortgage→MORTGAGE, Car→AUTO, Student→STUDENT_LOAN, etc.; GoalsScreen homeowner check now true for a mortgage → 36% guideline |
| B-32 | LOW | Retirement copy | "You plan to spend $8,288/mo" — user said $15,000; Portugal COL ×0.6 applied silently on the cockpit | retirementSpendMonthly COL factor unlabeled outside onboarding | `screens_deep.test.tsx` "recognized retirement location labels the cost-of-living adjustment" + US-no-note | **fixed** 2026-06-13 | Cockpit now shows "Adjusted for Portugal's cost of living (40% lower than the US)" when a recognized location moves the figure; US/factor-1 shows no note |
| B-36 | LOW | Tool prefills | Insurance check (savings/assets $0 → "$3.5M gap" for a 74yo with $1.75M), Roth converter (pre-tax $0 despite known $250k; misleading zero-state message) | Tools don't prefill from store/onboarding | `tools_prefill.test.tsx` (insurance savings + Roth pre-tax) | **fixed** 2026-06-13 | Insurance prefills savings/assets from investableValue; Roth pre-tax balance filter fixed PRE_TAX (was a non-existent 'RETIREMENT' bucket → always $0) |
| B-37 | MED | Retirement framing | Retiree cockpit showed accumulation framing ("Grow my nest egg · assumes age 65") for a retired 74yo, contradicting Home's "drawdown" focus | No `targetRetirementAge` → defaulted 65; plan divider + basis label were accumulation-worded for everyone | `screens_deep.test.tsx` "retired user sees 'in retirement', not 'assumes age 65'" + accumulating-keeps-age-framing | **fixed** 2026-06-13 | Retiree's retire-age default is now their current age (not 65); plan divider shows "in retirement" and the return label "Assumed return on what's left" when retired. (Drawdown heroes/section already gated on isRetired.) |
| B-23 | LOW | Currency | A Settings currency picker let users choose EUR/GBP/INR/etc., but all tax & retirement math is hardcoded US (federal brackets, 401k/IRA/HSA limits, RMD age 73, 529s, single-filer) — picking a non-USD currency only swapped the symbol while still applying US tax math | Cross-cutting; picker in `SettingsScreen.tsx` | `_shared/money.test.ts` currency-seam test | **fixed** 2026-06-13 | Removed the currency picker; `app/_layout.tsx` forces `setMoneyFormat('USD','en-US')` so a stale synced currency can't desync the formatter. Currency plumbing (money.ts/CURRENCIES/store fields/setCurrency) left dormant for a future per-country tax epic. The ~500 hardcoded `$` and `AmountText`'s USD hardcode are now correct, so untouched (avoids needless churn) |
| B-38 | MED | Retirement realism | RMDs (forced withdrawals age 73+) are computed in `decumulation` but `simulate()` never forces them — post-73 mandatory withdrawals & their tax drag aren't modeled in the Monte Carlo | `src/domain/retirement/index.ts` `simulate()` has no RMD term (decumulation `rmdAtAge` unused there) | prior data-review §2 | **fixed (transparency)** 2026-06-13 | The drawdown view already shows RMDs ($/yr from age 73, "taxed whether you need it or not"); added that the success-chance figure tracks balances BEFORE tax, so set aside for the tax. (Modeling RMD tax inside the MC needs a retirement-tax-rate assumption — deferred to avoid churn in a sensitive engine.) |
| B-39 | MED | Retirement realism | Retirement projection ignores `liabilities` — it implicitly assumes debt-free at retirement; a user with a mortgage sees an over-optimistic "needed" | `buildRetirementState` start_balance = assets only (`src/domain/snapshot.ts:70`); no debt term in retirement inputs | prior data-review §2 | **fixed (transparency)** 2026-06-13 | `screens_deep.test.tsx` "user with debt sees the note". A user with debt is now told the projection assumes debt is covered within spending — include an ongoing mortgage payment in the spend figure (rather than silently assuming debt-free). |
| B-40 | MED | Edit-back coupling | Income/cashflow read raw `onboardingProfile`, so editing income after onboarding doesn't reflect unless the edit writes back into `op` — Net Worth/Budget edits write their own store arrays that income/cashflow don't read | Architectural: domain reads `op` for income; post-onboarding income edits weak spot | prior data-review "Cons" | **fixed** 2026-06-13 | IncomeManager writes edits back to onboardingProfile (verified); fixed the concrete gap where editing base pay to a flat amount left a per-month salary table in place (table takes precedence → edit was silently ignored) — now clears salaryByMonth |
| B-41 | LOW | Goals feedback | `goals.waterfall()` recommends a funding order but nothing records what actually got funded — suggestion-only, no feedback loop | `src/domain/goals/index.ts` waterfall is stateless | prior data-review §2 | closed — not a bug | Verified 2026-06-13: `waterfall()` is defined+tested but NOT imported/rendered by any screen, so there is no user-facing "suggestion without feedback" gap. Available for future use |
| B-42 | LOW | Legacy field | Legacy `debts` array still present in store (`useStore.ts:208,395`) alongside canonical `liabilities`; @deprecated + migration landing, not yet removed | Dual representation kept for migration | prior data-review §5 | deferred | Not a user-facing bug: `@deprecated`, unsynced, and BudgetScreen migrates it into `liabilities` on mount. It IS the migration landing spot — removing it now would drop legacy data and benefits nobody. Remove post-launch once no legacy data exists |
| B-43 | LOW | Transaction UI | TransactionSheet account chips render identically when two accounts share a name (no kind/last-4 disambiguator) | `PerformanceScreen` TransactionSheet chip label = account name only | prior Tier-4 flow walkthrough observation | **fixed** 2026-06-13 | Transaction-sheet chips append the asset kind (or id last-4) when two accounts share a name; smoke-rendered |
| B-44 | LOW | Copy | "Your cash covers about 0.0 months of spending" reads robotically and alarms a user with large liquid investments but $0 in a cash bucket | Home + Net Worth emergency-fund insight copy (cash-only runway) | `insights.test.ts` "zero-cash runway says no cash set aside" | **fixed** 2026-06-13 | Insights + Net Worth runway say "No cash set aside" at ~0 and note investments aren't counted |
| B-45 | LOW | Copy | "86% of your portfolio is a single position" — it's a single account, not a holding | Insights concentration rule measured the largest ACCOUNT | `insights.test.ts` "concentration says account not position" | **fixed** 2026-06-13 | Retitled "Concentrated in one account"; `topHoldingPct`→`topAccountPct`; body says add holdings + spread across accounts |
| B-46 | LOW | Copy | Tips screen "This month at a glance: Income $0.00" contradicts a declared-$276k user; also uses cents against the app-wide whole-dollar style | TipsScreen month-at-a-glance reads logged transactions only | TipsScreen at-a-glance | **fixed** 2026-06-13 | Added "From what you've logged this month — not your full income or budget" subtitle |
| B-47 | LOW | Copy | Student's $400/mo parental support labeled "Child support / alimony" in the income manager | The `supportMonthly` onboarding question IS "Child support or alimony" (modules.tsx:549) — label matches the field | persona walk TC-S4 | closed — not a bug | Verified 2026-06-13: label is consistent with the question; the persona fixture mis-used the field. (Minor future enhancement: a dedicated "family/parental support" field — not a defect) |
| B-48 | LOW | Copy | "Room left in your 401(k)" nudge shown to users with no earned income (retiree, gig) | Insights 401(k)-room rule not gated on earned income | `insights.test.ts` "401(k)-room nudge requires earned income" | **fixed** 2026-06-13 | Added `hasEarnedIncome` (salaryAnnual > 0) gate to the rule |
| B-49 | MED-HIGH | Cross-module: assets basis | `buildSnapshot` derives net worth + retirement start_balance from `assetsFromOnboarding(op)` (onboarding answers), but the Net Worth screen, TopBar chip, and Retirement cockpit use the LIVE `store.assetAccounts`. After any account add/edit/delete, the two bases diverge — Home's "Net worth · see all plans" (op) ≠ TopBar chip (accounts); Home retirement outlook (op) ≠ cockpit nest egg (accounts) | `snapshot.ts:52,70` vs `RetirementCockpit.tsx:38,49` / `HomeScreen.tsx:116` vs `:492` | audit harness 2026-06-13 (snapshot NW −$6k vs accounts $98k; earmarked $0 vs $102k) | **fixed** 2026-06-20 (verified) | RESOLVED by the `resolveNetWorthRows` refactor: `buildSnapshot` now REQUIRES explicit account/liability rows (no silent op fallback), and Home, TopBar, and the Retirement cockpit all derive rows from `resolveNetWorthRows` (live store accounts once seeded/non-empty; nest egg = `retirementEarmarkedValue` of those same rows). One basis everywhere. Locked by tests: `home_integration.test.ts`, `journeys.test.ts` (B-49), `portfolio_usecases.test.ts` (nest-egg basis), `invariants.test.ts` (retirement starts from the same total the NW chip shows). |
| B-50 | MED | Cross-module: emergency runway | The emergency-fund runway ("cash covers X months") uses `spendBuckets.monthly_total || monthlySpending` (bucket if itemized, else stated), but the budget's `monthly_spending` is now `max(bucket, stated)` (B-24). A user who states a total but itemizes part gets a runway 2× too long: harness shows "1.6 months" where the true budget spend gives 0.8 | `InsightsScreen.tsx:26` + `NetWorthScreen.tsx:70` use the old `bucket\|\|stated`; `budget/index.ts` uses `max` | `budget.test.ts` "plannedMonthlySpend (B-50)" | **fixed** 2026-06-13 | New exported `plannedMonthlySpend(op)` = max(bucket, stated); budgetFromOnboarding, monthlyEssentials, and both runway screens route through it → runway denominator now equals budget.monthly_spending |
| B-51 | MED | Cross-module: toxic-debt threshold | "High-interest/toxic debt" is defined with 3 thresholds: insights "toxic-debt" rule fires at APR > 10% (`InsightsScreen.tsx:36`), but the debt module's `toxic_debt_balance` and the Net Worth "costliest" use > 7% (`debt/index.ts:124`, `NetWorthScreen.tsx:80`). An 8% debt is flagged toxic in Net Worth + counted in toxic_debt_balance, yet the Home "tackle high-interest debt" insight stays silent | threshold drift across 3 files | `insights_screen.test.tsx` "8% debt fires the high-interest-debt insight" | **fixed** 2026-06-13 | Exported `TOXIC_APR` (0.07) from debt module; Insights + Net Worth now import it (was a hardcoded 0.10 in Insights). One threshold everywhere |
| B-52 | LOW-MED | Cross-module: "savings rate" | Two different numbers carry the name "savings rate": budget `savings_rate_pct` = (net income − spending)/net income (21.9% for the audit persona), while the insight's `savingsRate` = retirement contributions/gross (8.75%). Same term, ~2.5× apart → confusing | `budget/index.ts:37` vs `InsightsScreen.tsx:44` | audit harness 2026-06-13 | **fixed** 2026-06-20 | Disambiguated: the Insights metric was renamed `savingsRate`→`investRate` and now reads "investing X% of your **gross** income toward retirement" (`insights/index.ts` invest-rate rule). The Analytics "Savings rate" stat is now labelled **"Savings rate (of take-home)"** with a caption ("Share of your take-home pay you keep; investing as a % of gross is shown in Insights"), and the avg / by-month labels say "(take-home)". Two clearly distinct numbers, two clearly distinct labels. |
| B-53 | CRITICAL | Security | Firestore rules let ANY signed-in user read/write ANY other user's data: `isHouseholdMember(victim)` only checked the *caller's own* doc `householdId == victim`, and a user can write their own doc freely → setting `householdId=<victimUID>` self-granted full access. Invite-code gate existed only in client code, not rules | `firestore.rules` old `isHouseholdMember` | `firestore.rules.test.cjs` (emulator) "THE OLD EXPLOIT: self-claiming householdId does NOT grant access" | **fixed + DEPLOYED** 2026-06-17 | Grant now owner-side + invite-bound: join via `households/{owner}/members/{me}` carrying a `code` the rules verify; access gated on that member doc. 8/8 emulator tests; deployed to finwise-app-jj |
| B-54 | HIGH | Cross-module: nest egg | Retirement cockpit showed **$0 nest egg despite $2.5M** net worth — read empty legacy `store.savings`/`store.investments` arrays instead of the seeded earmarked accounts the Home NW box uses (a 3rd assets source; cousin of B-49) | `RetirementCockpit.tsx` `useStore().{savings,investments}` | retired-persona test 2026-06-17 | **fixed** 2026-06-17 (ba999c1) | Cockpit now reads `resolveNetWorthRows(uid, op, nwSeeded, assetAccounts, liabilities).accounts` → `retirementEarmarkedValue(...)`, same seeded source as Home |
| B-55 | MED | Income/tax | Onboarding cash-flow recap (retired) used **gross** retirement income ($24,333) while the spending-plan step used **take-home** ($18,889); "left over" computed off pre-tax → screens disagreed | `modules.tsx` CashflowRecap retired branch `retirementMonthlyIncome(a) \|\| monthlyIncome(a)` (gross first) vs spending plan's net-first | retired-persona test 2026-06-17 | **fixed** 2026-06-17 (ba999c1) | Retired branch now `monthlyIncome(a) \|\| retirementMonthlyIncome(a)` (net, matches spending plan) + spend uses max(buckets,stated); labeled "Take-Home (After Tax)" |
| B-56 | MED | Retirement | Legacy bequest (the "leave to heirs" target) was ignored in the drawdown — "will it last" drew down the full nest egg, not the spendable remainder | `RetirementCockpit.tsx` used `nestEgg` for lasts/withdrawal calcs | retired-persona test 2026-06-17 | **fixed** 2026-06-17 (ba999c1) | New `spendableEgg = max(0, nestEgg − legacyTarget)` threaded into the retired chance/withdrawal/depletion calcs; verdict shows the reserve |
| B-57 | HIGH | Build / native | `react-native-worklets` peer dep missing (required by reanimated v4) → app can crash outside Expo Go, i.e. in the TestFlight/production build (passes in dev) | `package.json` | `npx expo-doctor` ("required peer dependencies") | **fixed** 2026-06-17 (a99e2aa) | `npx expo install react-native-worklets` (0.8.3); babel-preset-expo auto-wires the plugin |
| B-58 | MED | Retirement / decumulation | Suggested **withdrawal order was static, not RMD-aware** — `withdrawalOrder(split)` ignored age, listing pre-tax last even for an 80-year-old whom RMDs legally force to withdraw from pre-tax FIRST | `decumulation/index.ts` `withdrawalOrder(split)` (no age) | `decumulation.test.ts` "RMD-age users get the required withdrawal as step 1" | **fixed** 2026-06-17 | Added `age` param; for age≥73 with pre-tax, prepends a mandatory RMD step (amount = `rmdAtAge`), pre-tax step relabeled "the rest"; under-73 unchanged |
| B-59 | LOW-MED | Copy (age-blind) | Retirement "Heads-up" copy was static & ignored age — "Medicare starts at 65" shown to an 80-year-old; RMD note said "start at 73 / first year" for someone a decade past RMD age | `RetirementCockpit.tsx` heads-up card hardcoded strings | retire-screen review 2026-06-17 | **fixed** 2026-06-17 | Copy now branches on `age`: ≥65 → "you're on Medicare … watch IRMAA"; ≥73 → "RMDs apply now — you must withdraw ~$X this year". (Pattern flag: audit other screens for age-blind static copy) |
| B-60 | HIGH | Data loss: assets balance | Adding **one** holding to a manual-balance account to track its performance **collapsed the whole account to that one holding** — a ~$2.2M Fidelity total dropped to $179k (the single stock) after a price refresh. Any account with ≥1 position was treated as fully position-derived | `useStore.ts` `recomputeBalances` flagged `ledgerManaged = positions.length>0 \|\| cash_balance!=null`, then overwrote `balance = cash + Σpositions` | `store_prices.test.ts` "manual-balance account with a partially-tracked holding (B-60)" (3 cases) | **fixed** 2026-06-18 | New explicit `derive_balance` flag on `AssetAccount`: balance is rebuilt from Σpositions ONLY when `derive_balance===true` (a brokerage built from its holdings — set on the Performance "new Brokerage" path) or a cash sleeve is set. A manual-balance account keeps its entered total; its positions are a SUBSET tracked for performance only. NOTE: already-clobbered balances can't be auto-restored — affected users must re-enter the account total once; it will no longer be wiped |
| B-61 | LOW | UX / discoverability | **Tax organizer buried** deep in the Plan-screen module grid despite being a critical feature for many users | nav / `TopBar.tsx` | UI feedback 2026-06-18 | **fixed** 2026-06-18 (dd506fe) | Added a global **Tax organizer chip stacked under the NW chip** in TopBar — visible on every tab page; equal-sized to the NW chip. `TopBar.test.tsx` covers its navigation |
| B-62 | LOW | UX / layout | **Bill calendar** was a standalone card on Home, separate from the quick-action "focus" box | `HomeScreen.tsx` | UI feedback 2026-06-18 | **fixed** 2026-06-18 (dd506fe) | Moved Bill calendar **into the persona focus box** (all personas); removed the standalone card |
| B-63 | LOW | UX / layout | Retire drawdown showed **"Where your money sits" and "Suggested withdrawal order" as two separate cards** | `RetirementCockpit.tsx` | UI feedback 2026-06-18 | **fixed** 2026-06-18 (dd506fe) | Combined into **one card** split by a divider |
| B-64 | MED | Transparency | Retire "Suggested withdrawal order" showed an **RMD of ~$26K with no derivation**; steps 2–4 showed full bucket balances, so a user couldn't tell where $26K came from or whether it double-counted the pre-tax bucket | `RetirementCockpit.tsx` / `decumulation` | UI feedback 2026-06-18 | **fixed** 2026-06-18 (dd506fe) | RMD step now shows **"= your $X pre-tax ÷ <IRS life-expectancy factor> at age N"** + a note that the bucket amounts are balances while the RMD is just this-year's required slice of pre-tax (not money on top) |
| B-65 | LOW | UI layout | Retire "Your investments" table — the **"Your 12-mo" and "Benchmark" column headers had no gap** and ran together | `RetirementCockpit.tsx` `tHead`/`tRow` | UI feedback 2026-06-18 | **fixed** 2026-06-18 (dd506fe) | Added `gap:14` to the header + data rows so the two numeric columns separate |
| B-66 | LOW | UX / layout | Net Worth — **"Portfolio performance", "Bonds", "Other investments" rendered as three loose cards** floating mid-screen, feeling out of place | `NetWorthScreen.tsx` | UI feedback 2026-06-18 | **fixed** 2026-06-18 (dd506fe) | Grouped into one **"Explore your holdings"** box with dividers |
| B-67 | HIGH | Income / cross-screen | **"Take-home" showed two different numbers**: income recap $22,990/mo (after tax + 401k) vs spending plan $24,490 (before 401k) — off by exactly the 401(k); the budget then let you allocate spendable cash that's locked in the 401(k) | spending plan + savings editor displayed `monthlyIncome` (net of tax, before 401k) while the income screen used after-401k; no single take-home helper, and agreement tests were intra-domain not cross-screen (L-7) | `take_home_agreement.test.ts` | **fixed** 2026-06-23 | Canonical `takeHomeMonthly` (after tax AND 401k); income recap, spending plan, savings editor, retired cash-flow all read it; cross-screen agreement guard added |
| B-68 | HIGH | Savings rate / cross-screen | **Savings rate defined two ways.** AnalyticsScreen computes its OWN `(monthIncome − monthSpend) / monthIncome` from this-month logged data, while the canonical `savingsRateCash`/`savingsRateTotal` (Term #10) are **unwired** — so Analytics' rate ≠ onboarding's "% of take-home you save" | A-2 audit: bespoke inline calc on AnalyticsScreen; canonical helpers never wired into a screen | `number_agreement.test.ts` (B-68 guard) | **fixed** 2026-06-23 (9c8f025) | AnalyticsScreen now reads canonical `savingsRateCash(profile, liabilities)`; inline calc deleted; static guard added |
| B-69 | HIGH | Retirement / cross-screen | **Two retirement engines give different "are you on track" answers.** `RetirementScreen` uses a simple `income × years` target + compound FV; `RetirementCockpit` uses a Monte-Carlo chance-of-success. A user sees two different readiness verdicts | A-2 audit: two independent retirement calcs across two screens | _(A-2 matrix)_ | **open** (P0) | Reconcile to ONE engine (the Monte-Carlo cockpit) or clearly differentiate; agreement test |
| B-70 | MED | Investable / cross-screen | **Two "investable" definitions.** InsightsScreen uses `investableValue` (excludes only the PROPERTY tax-bucket); the taxonomy uses `investableAssets` (excludes real_estate + personal_property classes). A vehicle in a TAXABLE bucket diverges | A-2 audit: old vs new investable helper | `number_agreement.test.ts` (B-70 guard) | **fixed** 2026-06-23 (0bb7041) | Removed `investableValue`; Insights/Insurance/OtherInvestments + golden/edge tests use canonical `investableAssets`; guard bans re-introduction |
| B-71 | HIGH | Goals ↔ surplus integration (architecture) | **Goals are a standalone manual tracker, disconnected from surplus + real accounts.** Setting a goal (name/target/date) projects months-to-goal from capacity, but "saving" toward it is a self-reported `goal.saved` you type via "Add to goal" — NOT tied to any account/transaction. Separately, Home's "allocate surplus" routes to *accounts*, never to a goal. So surplus → goal funding → tracking is not one pipeline | A-2 follow-up trace (goals flow): `domain/goals`, `RewardsScreen.tsx`, store `allocateSavings`/`updateGoal` | `goals/funding.test.ts`, `goals_funding_integration.test.ts` | **fixed** 2026-06-25 (93b2b8c, mock-approved) | ONE pipeline: Home "Put your surplus to work" sheet has a GOALS section alongside accounts; `fundGoals(ym,items)` bumps `goal.saved` from real surplus + records it in `allocatedByMonth`; Rewards cards show "need $X/mo by <date>" + on-track/behind from `goalStatus`. Funding is manual-assign (user choice) |
| — | — | DECISION (surplus) | **Canonical "Surplus" = take-home − spending − debt (AFTER debt)**, shown as **"Actual surplus"** (this month, logged) and **"Planned surplus"** (from the plan). Applies to Home cash-flow box, Cash Flow detail, Budget. Resolves the B-67-family "Left over"≠"Surplus" word+number fork (Theme 1 P0) | user decision 2026-06-23 | `surplus_agreement.test.ts` | **implemented** 2026-06-24 (f0ed8ba) | New canonical `surplusByMonth`; CashFlow now after-debt + "Planned surplus"; Budget canonical + "Planned surplus"; Home residual → "Surplus"; recap relabeled; 7 agreement tests |
| B-72 | HIGH | Budget / 401(k) overstatement | **Budget overstated surplus by the monthly 401(k).** "Your monthly plan" used `incomeMonthlyGrid('net')` (after tax, BEFORE 401k) but **labeled it "Income (take-home)"** and subtracted spend+debt — so "Left over to save" counted 401(k) money as free cash (same class as B-67) | found during surplus reconciliation: BudgetScreen `planIncome` used 'net' to "mirror Home's baseNet" | `surplus_agreement.test.ts` (Budget guard) | **fixed** 2026-06-24 (f0ed8ba) | planIncome → canonical `takeHomeMonthly` (after tax + 401k); planExpenses → `plannedMonthlySpend`; plan total now == `monthlySavings` |

## Device-test build #33 (v1.0.8) findings → fixes (2026-06-26, branch taxonomy-v1.0.7)

All from the real-device test of build #33 (`docs/finwise-device-test-build33.xlsx/.pdf`). Fixed one-at-a-time with a test + the quality gate; re-test sheet = `docs/finwise-device-test-build34.xlsx`. **T22 (B-89) was a real route-guard bug, not device-only — and it also fixes the new /contribution-room screen.**

| ID | Sev | Area | Symptom | Root cause | Fix (commit) |
|----|-----|------|---------|-----------|--------------|
| B-73 | HIGH | Goals | Goal status always green — never showed "behind" | /goals card hardcoded bar to `g.color`; on-track logic keyed off `targetDate` but card uses `duration` | `goalStatus` + duration fallback + amber bar + chip (b27cd01) |
| B-74 | HIGH | Privacy | Hide-balances masked only the NW chip, not other amounts | most screens call `money()` directly, not `AmountText` | mask at the formatter (`setHideBalances` module flag) + remount on toggle (35008ef) |
| B-75 | MED | Budget | "% of take-home" category had no way to enter a % | limits modal saved every category as `unit:'dollar'` | $/% toggle in the limits modal (fde8f76) |
| B-76 | LOW | Budget | Two "Set monthly limits" buttons | duplicate bottom button | removed the dupe (fde8f76) |
| B-77 | MED | Forms | Keyboard hid the bottom field on short sheets | KeyboardAvoidingView padding can't lift when content fits | iOS `automaticallyAdjustKeyboardInsets` (75bc54d) |
| B-78 | MED | Net Worth | Alternatives showed as "Other"; "By account" showed classes | rows used KIND label; group toggle mislabeled | `ASSET_CLASS_LABEL[assetClassOf(a)]` + true "By institution" view (9161626) |
| B-79 | LOW | Net Worth | "Portfolio performance" not taxonomy "Stocks/ETFs" (repeat) | missed first pass | row → "Stocks / ETFs — performance vs benchmark" (808f788) |
| B-80 | HIGH | Net Worth | Entering an institution on an option overwrote the ticker/label ("options→chase") | Alternatives editor had NO institution field — user typed it into the only field (label) | added "Where it's held (institution)" field (808f788) |
| B-81 | HIGH | Performance | Cost/share + Shares rejected decimals; share count showed float drift | inputs parsed on every keystroke and wrote the number back to the controlled value | raw-text buffer `lotRaw`; share count `maximumFractionDigits:4` (2b0ad24) |
| B-82 | MED | Import | CSV preview blank for CDs/bonds/money-market | preview rendered only the ticker | show ticker‖description‖symbol + asset class per row (1417670) |
| B-83 | HIGH | Insights | "401(k) room left" dumped you in the Retire cockpit | wrong route; no contribution detail existed | built **/contribution-room** screen (401k/IRA/HSA limit·used·room·how-to-add); reroute (5769c6a) |
| B-84 | MED | Insights | Numbers unexplained; flat list | no provenance; no grouping | drill-down sheet (accounts+math) per insight + Protect/Grow/Optimize themes (6d99d7e) |
| B-85 | LOW | Insights | "Retirement needs attention" card never appeared | `retireChance` hardcoded null (heavy sim skipped) | cockpit caches its Monte-Carlo chance (`lastRetireChance`); Insights reads it (6d99d7e) |
| B-86 | HIGH | Net Worth | Bonds/Alternatives had no way to record a sale | modeled as manual-balance only — but bonds sell on secondary markets, American options sell before expiry | "Record a sale" on both: full closes, partial lowers value (bond face scales) (c5bfe3b) |
| B-87 | MED | Net Worth | Imported stock LCTX buried under "Imported holdings" | equities import as one brokerage account with positions inside | NW row surfaces held tickers; single-equity import names the account after the ticker (752311a) |
| B-88 | LOW | Education | No InfoDots for asset classes / income | glossary lacked the terms | +8 glossary terms; InfoDots on NW class headers, Bonds, Alternatives, Contribution-room (df6cf08) |
| B-89 | HIGH | Navigation (T22) | "Cash-flow detail →" loops back to Home | route guard `router.replace()`s any segment not in `MODAL_SEGMENTS`; `cashflow` (+ `contribution-room`) were missing | whitelisted both + filesystem regression test that every root route is whitelisted (8f82c00) |
